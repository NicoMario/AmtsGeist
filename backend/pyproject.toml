[project]
name = "amtsgeist-backend"
version = "0.1.0"
description = "Souveränes Inferenz-Backend für den AmtsGeist-Outlook-Assistenten"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
